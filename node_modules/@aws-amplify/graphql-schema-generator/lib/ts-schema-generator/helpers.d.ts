import ts from 'typescript';
import { VpcConfig } from '@aws-amplify/graphql-transformer-interfaces';
import { Schema } from '../schema-representation';
export type DataSourceConfig = {
    secretName: string;
    identifier: string;
    vpcConfig?: VpcConfig;
};
export declare const createSchema: (schema: Schema, config?: DataSourceConfig) => ts.Node;
export declare const createImportExpression: (containsSecretName: boolean) => ts.NodeArray<ts.ImportDeclaration>;
export declare const createConfigureExpression: (schema: Schema, config: DataSourceConfig) => ts.Expression;
//# sourceMappingURL=helpers.d.ts.map